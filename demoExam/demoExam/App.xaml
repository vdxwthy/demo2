<Application x:Class="demoExam.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:demoExam"
             StartupUri="Windows/MainShopWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="MainBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryBackground" Color="#7FFF00"/>
        <SolidColorBrush x:Key="Accent" Color="#00FA9A"/>
        <FontFamily x:Key="GlobalFont">Times New Roman</FontFamily>

        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
            <Setter Property="Background" Value="{StaticResource MainBackground}"/>
        </Style>

        <Style TargetType="Page">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
            <Setter Property="Background" Value="{StaticResource MainBackground}"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
            <Setter Property="Background" Value="{StaticResource Accent}"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
        </Style>
        
        <Style TargetType="Label">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
        </Style>
    </Application.Resources>
</Application>
